*** Settings ***
Library           ExtendedSelenium2Library

*** Variables ***
${Browser}        Firefox
${SiteUrl}        http://0.0.0.0:8100/

*** Test Cases ***
test
    open browser    ${SiteUrl}    ${Browser}
    wait until page contains     Login
    close browser
