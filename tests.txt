*** Settings ***
Library           Selenium2Library

*** Variables ***
${Browser}        Firefox
${SiteUrl}        http://bowsse.github.io/webdev

*** Test Cases ***
test
    open browser    ${SiteUrl}    ${Browser}
    wait until page contains     Bowsse
    close browser
